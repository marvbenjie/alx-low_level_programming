#include "main.h"

/**
 * _abs - computes the absolute value
 * @n: the int in question
 *Return: The absolute value of interger
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}

